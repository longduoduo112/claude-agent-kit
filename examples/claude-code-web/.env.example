PORT=5173

# Required – replace with your Claude/Anthropic-compatible key
ANTHROPIC_API_KEY=replace-with-your-key

# Optional overrides when using Anthropic-compatible providers (e.g., GLM)
ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic
ANTHROPIC_API_URL=https://open.bigmodel.cn/api/anthropic
ANTHROPIC_MODEL=glm-4.6

# Optional – enable Jina MCP server (https://mcp.jina.ai)
JINA_API_KEY=replace-with-your-jina-key

# Optional – secure the /api surface
API_AUTH_TOKEN=change-me
# Rate limiting (defaults: 15m window, 300 reqs)
API_RATE_LIMIT_WINDOW_MS=900000
API_RATE_LIMIT_MAX=300
